# DistillFW — Project Agent Context & Guidelines

- Always read spec/main.md for detailed specifications.
- If I ask you to fix, improve or change anything, (1) update spec/main.md accordingly so that whenever I run the whole process again from scratch it works as intended as a whole,  (2) update the guides under docs/ so that they can be followed exactly and with no mismatches with respect to the implementation and (3) make a git commit with a descriptive message, followed by a git push.
